services:
  weather-tracker:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - weather-data:/data
    env_file:
      - .env
    environment:
      # Override the .env value so the container always writes to the mounted volume.
      DATABASE_URL: sqlite:////data/weather.db

volumes:
  weather-data:
